# ThinkFlow 数据库集成实施计划

## 📋 项目概述

**目标**: 将 ThinkFlow 从 localStorage 迁移到 Supabase 数据库，实现多用户数据隔离和持久化存储。

**当前状态**: 
- ✅ 前端功能完整（文件上传、拖放、知识图谱等）
- ✅ 后端 API 服务已搭建
- ❌ 数据存储在 localStorage（单机、易丢失）
- ❌ 无用户认证系统
- ❌ 无法支持多用户

**目标状态**:
- ✅ Supabase 数据库存储
- ✅ Supabase Auth 用户认证
- ✅ 多用户数据隔离
- ✅ 跨设备数据同步
- ✅ 生产环境就绪

---

## 🎯 实施原则

1. **先数据库，后部署** - 核心功能先完成，再考虑部署
2. **渐进式迁移** - 保持功能可用，逐步替换
3. **数据安全** - 使用 Row Level Security (RLS) 确保数据隔离
4. **向后兼容** - 支持从 localStorage 迁移现有数据

---

## 📊 数据库表设计

### 1. 用户表 (users)
**说明**: Supabase Auth 自动提供，无需手动创建
- `id` (uuid) - 用户ID，主键
- `email` (text) - 邮箱
- `user_metadata` (jsonb) - 额外信息（如 full_name）

### 2. 对话表 (conversations)
```sql
create table conversations (
  id uuid default gen_random_uuid() primary key,
  user_id uuid references auth.users(id) on delete cascade not null,
  title text not null,
  created_at timestamptz default now() not null,
  updated_at timestamptz default now() not null
);

-- 索引
create index conversations_user_id_idx on conversations(user_id);
create index conversations_updated_at_idx on conversations(updated_at desc);

-- RLS 策略
alter table conversations enable row level security;

create policy "Users can view their own conversations"
  on conversations for select
  using (auth.uid() = user_id);

create policy "Users can insert their own conversations"
  on conversations for insert
  with check (auth.uid() = user_id);

create policy "Users can update their own conversations"
  on conversations for update
  using (auth.uid() = user_id);

create policy "Users can delete their own conversations"
  on conversations for delete
  using (auth.uid() = user_id);
```

### 3. 笔记表 (notes)
```sql
create table notes (
  id uuid default gen_random_uuid() primary key,
  conversation_id uuid references conversations(id) on delete cascade not null,
  user_id uuid references auth.users(id) on delete cascade not null,
  title text,
  content text,
  x numeric default 0,
  y numeric default 0,
  parent_id uuid references notes(id) on delete set null,
  updated_at timestamptz default now() not null
);

-- 索引
create index notes_conversation_id_idx on notes(conversation_id);
create index notes_user_id_idx on notes(user_id);
create index notes_parent_id_idx on notes(parent_id);

-- RLS 策略
alter table notes enable row level security;

create policy "Users can view their own notes"
  on notes for select
  using (auth.uid() = user_id);

create policy "Users can insert their own notes"
  on notes for insert
  with check (auth.uid() = user_id);

create policy "Users can update their own notes"
  on notes for update
  using (auth.uid() = user_id);

create policy "Users can delete their own notes"
  on notes for delete
  using (auth.uid() = user_id);
```

### 4. 消息表 (messages)
```sql
create table messages (
  id text primary key, -- 使用前端生成的ID格式: timestamp + '-user' 或 timestamp + '-ai'
  conversation_id uuid references conversations(id) on delete cascade not null,
  user_id uuid references auth.users(id) on delete cascade not null,
  role text not null check (role in ('user', 'ai')),
  agent text, -- AgentType: 'Manager', 'Socrates', etc.
  content text not null,
  thought_log jsonb, -- 存储 thoughtLog 数组
  referenced_files jsonb, -- 存储 referencedFiles 数组
  timestamp bigint not null,
  is_streaming boolean default false,
  created_at timestamptz default now() not null
);

-- 索引
create index messages_conversation_id_idx on messages(conversation_id);
create index messages_user_id_idx on messages(user_id);
create index messages_timestamp_idx on messages(timestamp);

-- RLS 策略
alter table messages enable row level security;

create policy "Users can view their own messages"
  on messages for select
  using (auth.uid() = user_id);

create policy "Users can insert their own messages"
  on messages for insert
  with check (auth.uid() = user_id);

create policy "Users can update their own messages"
  on messages for update
  using (auth.uid() = user_id);

create policy "Users can delete their own messages"
  on messages for delete
  using (auth.uid() = user_id);
```

### 5. 文件表 (uploaded_files)
```sql
create table uploaded_files (
  id text primary key, -- 使用前端生成的ID格式
  conversation_id uuid references conversations(id) on delete cascade not null,
  user_id uuid references auth.users(id) on delete cascade not null,
  name text not null,
  type text not null check (type in ('pdf', 'txt', 'md', 'code', 'other')),
  size bigint not null,
  content text, -- 文本文件内容
  file_uri text, -- Gemini File API URI (如果使用)
  mime_type text,
  uploaded_at bigint not null, -- 使用 timestamp
  created_at timestamptz default now() not null
);

-- 索引
create index uploaded_files_conversation_id_idx on uploaded_files(conversation_id);
create index uploaded_files_user_id_idx on uploaded_files(user_id);

-- RLS 策略
alter table uploaded_files enable row level security;

create policy "Users can view their own files"
  on uploaded_files for select
  using (auth.uid() = user_id);

create policy "Users can insert their own files"
  on uploaded_files for insert
  with check (auth.uid() = user_id);

create policy "Users can update their own files"
  on uploaded_files for update
  using (auth.uid() = user_id);

create policy "Users can delete their own files"
  on uploaded_files for delete
  using (auth.uid() = user_id);
```

### 6. 用户偏好表 (user_preferences) - 可选
```sql
create table user_preferences (
  user_id uuid references auth.users(id) on delete cascade primary key,
  layout_left_open boolean default true,
  layout_right_open boolean default true,
  updated_at timestamptz default now() not null
);

-- RLS 策略
alter table user_preferences enable row level security;

create policy "Users can manage their own preferences"
  on user_preferences for all
  using (auth.uid() = user_id);
```

---

## 🔧 实施步骤

### 阶段 1: 环境准备 (30分钟)

#### 1.1 安装 Supabase 客户端
```bash
cd frontend
npm install @supabase/supabase-js
```

#### 1.2 创建 Supabase 项目
1. 访问 https://supabase.com
2. 注册/登录账号
3. 创建新项目
4. 记录项目 URL 和 anon key

#### 1.3 配置环境变量
创建 `frontend/.env`:
```env
VITE_GEMINI_API_KEY=your_gemini_key
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key
```

#### 1.4 创建 Supabase 客户端文件
创建 `frontend/src/lib/supabaseClient.ts`:
```typescript
import { createClient } from '@supabase/supabase-js'

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY

if (!supabaseUrl || !supabaseAnonKey) {
  throw new Error('Missing Supabase environment variables')
}

export const supabase = createClient(supabaseUrl, supabaseAnonKey)
```

---

### 阶段 2: 数据库初始化 (20分钟)

#### 2.1 在 Supabase 中执行 SQL
1. 登录 Supabase Dashboard
2. 进入 SQL Editor
3. 依次执行上述所有表的创建 SQL
4. 验证 RLS 策略已启用

#### 2.2 测试数据库连接
创建测试脚本 `frontend/src/lib/testDb.ts` (临时):
```typescript
import { supabase } from './supabaseClient'

export async function testConnection() {
  const { data, error } = await supabase.from('conversations').select('count')
  if (error) {
    console.error('Database connection failed:', error)
    return false
  }
  console.log('Database connection successful!')
  return true
}
```

---

### 阶段 3: 用户认证迁移 (1小时)

#### 3.1 修改 AuthPage.tsx
**文件**: `frontend/src/components/AuthPage.tsx`

**主要改动**:
- 移除 localStorage 用户存储逻辑
- 使用 `supabase.auth.signInWithPassword()`
- 使用 `supabase.auth.signUp()`
- 处理邮箱验证流程

**关键代码**:
```typescript
import { supabase } from '../lib/supabaseClient'

// 登录
const { data, error } = await supabase.auth.signInWithPassword({
  email,
  password,
})

// 注册
const { data, error } = await supabase.auth.signUp({
  email,
  password,
  options: {
    data: { full_name: name }
  }
})
```

#### 3.2 修改 App.tsx 根组件
**文件**: `frontend/src/App.tsx`

**主要改动**:
- 移除 localStorage 用户读取
- 添加 Supabase Auth 状态监听
- 处理 session 管理

**关键代码**:
```typescript
useEffect(() => {
  // 获取当前 session
  supabase.auth.getSession().then(({ data: { session } }) => {
    if (session?.user) {
      setUser({
        id: session.user.id,
        email: session.user.email!,
        name: session.user.user_metadata?.full_name || 'User'
      })
    }
  })

  // 监听 auth 状态变化
  const { data: { subscription } } = supabase.auth.onAuthStateChange(
    (_event, session) => {
      if (session?.user) {
        setUser({
          id: session.user.id,
          email: session.user.email!,
          name: session.user.user_metadata?.full_name || 'User'
        })
      } else {
        setUser(null)
      }
    }
  )

  return () => subscription.unsubscribe()
}, [])
```

---

### 阶段 4: 对话管理迁移 (2小时)

#### 4.1 创建数据库服务层
创建 `frontend/src/services/conversationService.ts`:
```typescript
import { supabase } from '../lib/supabaseClient'
import { Conversation, Note, Message } from '../types'

// 获取用户的所有对话
export async function fetchConversations(userId: string): Promise<Conversation[]> {
  const { data, error } = await supabase
    .from('conversations')
    .select('*')
    .eq('user_id', userId)
    .order('updated_at', { ascending: false })

  if (error) throw error

  // 需要加载每个对话的 notes 和 messages
  const conversations: Conversation[] = []
  for (const conv of data || []) {
    const [notes, messages] = await Promise.all([
      fetchNotesForConversation(conv.id),
      fetchMessagesForConversation(conv.id)
    ])
    
    conversations.push({
      id: conv.id,
      title: conv.title,
      createdAt: new Date(conv.created_at).getTime(),
      updatedAt: new Date(conv.updated_at).getTime(),
      notes,
      messages
    })
  }

  return conversations
}

// 创建新对话
export async function createConversation(
  userId: string,
  title: string
): Promise<Conversation> {
  const { data, error } = await supabase
    .from('conversations')
    .insert({
      user_id: userId,
      title,
    })
    .select()
    .single()

  if (error) throw error

  return {
    id: data.id,
    title: data.title,
    createdAt: new Date(data.created_at).getTime(),
    updatedAt: new Date(data.updated_at).getTime(),
    notes: [],
    messages: []
  }
}

// 更新对话
export async function updateConversation(
  conversationId: string,
  updates: { title?: string }
): Promise<void> {
  const { error } = await supabase
    .from('conversations')
    .update({
      ...updates,
      updated_at: new Date().toISOString()
    })
    .eq('id', conversationId)

  if (error) throw error
}

// 删除对话
export async function deleteConversation(conversationId: string): Promise<void> {
  const { error } = await supabase
    .from('conversations')
    .delete()
    .eq('id', conversationId)

  if (error) throw error
}
```

#### 4.2 创建笔记服务
创建 `frontend/src/services/noteService.ts`:
```typescript
import { supabase } from '../lib/supabaseClient'
import { Note } from '../types'

// 获取对话的所有笔记
export async function fetchNotesForConversation(
  conversationId: string
): Promise<Note[]> {
  const { data, error } = await supabase
    .from('notes')
    .select('*')
    .eq('conversation_id', conversationId)
    .order('updated_at', { ascending: false })

  if (error) throw error

  return (data || []).map(n => ({
    id: n.id,
    title: n.title,
    content: n.content,
    x: n.x,
    y: n.y,
    parentId: n.parent_id,
    updatedAt: new Date(n.updated_at).getTime()
  }))
}

// 创建笔记
export async function createNote(
  conversationId: string,
  userId: string,
  note: Omit<Note, 'id' | 'updatedAt'>
): Promise<Note> {
  const { data, error } = await supabase
    .from('notes')
    .insert({
      conversation_id: conversationId,
      user_id: userId,
      title: note.title,
      content: note.content,
      x: note.x,
      y: note.y,
      parent_id: note.parentId
    })
    .select()
    .single()

  if (error) throw error

  return {
    id: data.id,
    title: data.title,
    content: data.content,
    x: data.x,
    y: data.y,
    parentId: data.parent_id,
    updatedAt: new Date(data.updated_at).getTime()
  }
}

// 更新笔记（使用 debounce）
let saveTimeout: NodeJS.Timeout | null = null
export async function updateNote(note: Note, userId: string): Promise<void> {
  // 清除之前的定时器
  if (saveTimeout) clearTimeout(saveTimeout)
  
  // 防抖：500ms 后保存
  saveTimeout = setTimeout(async () => {
    const { error } = await supabase
      .from('notes')
      .update({
        title: note.title,
        content: note.content,
        x: note.x,
        y: note.y,
        parent_id: note.parentId,
        updated_at: new Date().toISOString()
      })
      .eq('id', note.id)
      .eq('user_id', userId)

    if (error) {
      console.error('Error saving note:', error)
    }
  }, 500)
}

// 删除笔记
export async function deleteNote(noteId: string, userId: string): Promise<void> {
  const { error } = await supabase
    .from('notes')
    .delete()
    .eq('id', noteId)
    .eq('user_id', userId)

  if (error) throw error
}
```

#### 4.3 创建消息服务
创建 `frontend/src/services/messageService.ts`:
```typescript
import { supabase } from '../lib/supabaseClient'
import { Message } from '../types'

// 获取对话的所有消息
export async function fetchMessagesForConversation(
  conversationId: string
): Promise<Message[]> {
  const { data, error } = await supabase
    .from('messages')
    .select('*')
    .eq('conversation_id', conversationId)
    .order('timestamp', { ascending: true })

  if (error) throw error

  return (data || []).map(m => ({
    id: m.id,
    role: m.role as 'user' | 'ai',
    agent: m.agent as any,
    content: m.content,
    thoughtLog: m.thought_log || undefined,
    referencedFiles: m.referenced_files || undefined,
    timestamp: m.timestamp,
    isStreaming: m.is_streaming || false
  }))
}

// 创建消息
export async function createMessage(
  conversationId: string,
  userId: string,
  message: Omit<Message, 'id'>
): Promise<Message> {
  const { data, error } = await supabase
    .from('messages')
    .insert({
      id: message.id || `${Date.now()}-${Math.random().toString(36).substr(2, 9)}`,
      conversation_id: conversationId,
      user_id: userId,
      role: message.role,
      agent: message.agent,
      content: message.content,
      thought_log: message.thoughtLog || null,
      referenced_files: message.referencedFiles || null,
      timestamp: message.timestamp,
      is_streaming: message.isStreaming || false
    })
    .select()
    .single()

  if (error) throw error

  return {
    id: data.id,
    role: data.role as 'user' | 'ai',
    agent: data.agent as any,
    content: data.content,
    thoughtLog: data.thought_log || undefined,
    referencedFiles: data.referenced_files || undefined,
    timestamp: data.timestamp,
    isStreaming: data.is_streaming || false
  }
}

// 更新消息（用于流式更新）
export async function updateMessage(
  messageId: string,
  updates: Partial<Message>,
  userId: string
): Promise<void> {
  const { error } = await supabase
    .from('messages')
    .update({
      content: updates.content,
      thought_log: updates.thoughtLog || null,
      agent: updates.agent,
      is_streaming: updates.isStreaming || false
    })
    .eq('id', messageId)
    .eq('user_id', userId)

  if (error) throw error
}
```

#### 4.4 创建文件服务
创建 `frontend/src/services/fileDbService.ts`:
```typescript
import { supabase } from '../lib/supabaseClient'
import { UploadedFile } from '../types'

// 获取对话的所有文件
export async function fetchFilesForConversation(
  conversationId: string
): Promise<UploadedFile[]> {
  const { data, error } = await supabase
    .from('uploaded_files')
    .select('*')
    .eq('conversation_id', conversationId)
    .order('uploaded_at', { ascending: false })

  if (error) throw error

  return (data || []).map(f => ({
    id: f.id,
    conversationId: f.conversation_id,
    name: f.name,
    type: f.type as any,
    size: f.size,
    content: f.content,
    fileUri: f.file_uri,
    uploadedAt: f.uploaded_at,
    mimeType: f.mime_type
  }))
}

// 保存文件
export async function saveFile(
  conversationId: string,
  userId: string,
  file: UploadedFile
): Promise<UploadedFile> {
  const { data, error } = await supabase
    .from('uploaded_files')
    .insert({
      id: file.id,
      conversation_id: conversationId,
      user_id: userId,
      name: file.name,
      type: file.type,
      size: file.size,
      content: file.content,
      file_uri: file.fileUri,
      mime_type: file.mimeType,
      uploaded_at: file.uploadedAt
    })
    .select()
    .single()

  if (error) throw error

  return {
    id: data.id,
    conversationId: data.conversation_id,
    name: data.name,
    type: data.type as any,
    size: data.size,
    content: data.content,
    fileUri: data.file_uri,
    uploadedAt: data.uploaded_at,
    mimeType: data.mime_type
  }
}

// 删除文件
export async function deleteFile(
  fileId: string,
  userId: string
): Promise<void> {
  const { error } = await supabase
    .from('uploaded_files')
    .delete()
    .eq('id', fileId)
    .eq('user_id', userId)

  if (error) throw error
}
```

---

### 阶段 5: 修改 App.tsx 集成数据库 (3小时)

#### 5.1 修改 Workspace 组件初始化
**位置**: `frontend/src/App.tsx` - Workspace 组件

**主要改动**:
1. 移除所有 localStorage 读取逻辑
2. 使用数据库服务加载数据
3. 添加加载状态和错误处理

**关键代码**:
```typescript
// 替换初始化 useEffect
useEffect(() => {
  if (!user?.id) return

  const loadData = async () => {
    setIsLoading(true)
    try {
      // 加载对话列表
      const convs = await fetchConversations(user.id)
      setConversations(convs)
      
      if (convs.length > 0) {
        const activeId = localStorage.getItem('tf_active_conversation_id')
        const targetId = activeId && convs.find(c => c.id === activeId)
          ? activeId
          : convs[0].id
        loadConversation(targetId)
      } else {
        // 创建默认对话
        const newConv = await createConversation(user.id, '对话 1')
        setConversations([newConv])
        loadConversation(newConv.id)
      }
    } catch (error) {
      console.error('Error loading conversations:', error)
      setError('Failed to load conversations')
    } finally {
      setIsLoading(false)
    }
  }

  loadData()
}, [user])
```

#### 5.2 修改对话管理函数
- `createNewConversation()` - 调用 `createConversation()`
- `loadConversation()` - 从数据库加载 notes 和 messages
- `deleteConversation()` - 调用 `deleteConversation()`
- `updateActiveConversation()` - 调用 `updateConversation()`

#### 5.3 修改笔记管理函数
- `createDefaultNote()` - 调用 `createNote()`
- `handleCreateNote()` - 调用 `createNote()`
- `updateActiveNote()` - 调用 `updateNote()` (带防抖)
- `handleNoteMove()` - 调用 `updateNote()`
- `handleDeleteNote()` - 调用 `deleteNote()`

#### 5.4 修改消息管理
- `handleSendMessage()` - 调用 `createMessage()`
- `handleAIStream()` - 使用 `updateMessage()` 更新流式消息

#### 5.5 修改文件管理
- `processPendingFiles()` - 调用 `saveFile()`
- `handleDeleteFile()` - 调用 `deleteFile()`
- `loadConversation()` 中加载文件 - 调用 `fetchFilesForConversation()`

---

### 阶段 6: 数据迁移工具 (1小时)

#### 6.1 创建迁移脚本
创建 `frontend/src/utils/migrateLocalStorage.ts`:
```typescript
import { supabase } from '../lib/supabaseClient'
import { 
  createConversation, 
  createNote, 
  createMessage, 
  saveFile 
} from '../services'

export async function migrateLocalStorageToSupabase(userId: string) {
  // 检查是否已迁移
  const { data } = await supabase
    .from('conversations')
    .select('id')
    .eq('user_id', userId)
    .limit(1)

  if (data && data.length > 0) {
    console.log('Data already migrated')
    return
  }

  // 读取 localStorage 数据
  const storedConversations = localStorage.getItem('tf_conversations')
  if (!storedConversations) return

  const conversations = JSON.parse(storedConversations)

  // 迁移每个对话
  for (const conv of conversations) {
    // 创建对话
    const newConv = await createConversation(userId, conv.title)
    
    // 迁移笔记
    for (const note of conv.notes || []) {
      await createNote(newConv.id, userId, {
        title: note.title,
        content: note.content,
        x: note.x,
        y: note.y,
        parentId: note.parentId
      })
    }

    // 迁移消息
    for (const msg of conv.messages || []) {
      await createMessage(newConv.id, userId, {
        id: msg.id,
        role: msg.role,
        agent: msg.agent,
        content: msg.content,
        thoughtLog: msg.thoughtLog,
        referencedFiles: msg.referencedFiles,
        timestamp: msg.timestamp,
        isStreaming: msg.isStreaming
      })
    }

    // 迁移文件
    const storedFiles = localStorage.getItem(`tf_files_${conv.id}`)
    if (storedFiles) {
      const files = JSON.parse(storedFiles)
      for (const file of files) {
        await saveFile(newConv.id, userId, file)
      }
    }
  }

  console.log('Migration completed!')
}
```

#### 6.2 在 App.tsx 中调用迁移
在用户首次登录时自动迁移：
```typescript
useEffect(() => {
  if (user?.id) {
    // 检查是否有 localStorage 数据需要迁移
    const hasLocalData = localStorage.getItem('tf_conversations')
    if (hasLocalData) {
      migrateLocalStorageToSupabase(user.id).catch(console.error)
    }
  }
}, [user])
```

---

### 阶段 7: UI 状态管理 (30分钟)

#### 7.1 用户偏好
**选项 A**: 保留在 localStorage（推荐）
- 布局偏好（左右面板开关）是客户端设置
- 不需要跨设备同步
- 继续使用 localStorage

**选项 B**: 存储到数据库
- 如果希望跨设备同步布局设置
- 使用 `user_preferences` 表

#### 7.2 实现建议
保持布局偏好使用 localStorage，其他数据使用数据库。

---

### 阶段 8: 错误处理和优化 (1小时)

#### 8.1 添加错误边界
创建 `frontend/src/components/ErrorBoundary.tsx`:
```typescript
import React, { Component, ErrorInfo, ReactNode } from 'react'

interface Props {
  children: ReactNode
}

interface State {
  hasError: boolean
  error?: Error
}

export class ErrorBoundary extends Component<Props, State> {
  state: State = { hasError: false }

  static getDerivedStateFromError(error: Error): State {
    return { hasError: true, error }
  }

  componentDidCatch(error: Error, errorInfo: ErrorInfo) {
    console.error('Error caught by boundary:', error, errorInfo)
  }

  render() {
    if (this.state.hasError) {
      return (
        <div className="min-h-screen flex items-center justify-center">
          <div className="text-center">
            <h2 className="text-2xl font-bold text-red-600 mb-4">
              Something went wrong
            </h2>
            <button
              onClick={() => window.location.reload()}
              className="px-4 py-2 bg-indigo-600 text-white rounded"
            >
              Reload Page
            </button>
          </div>
        </div>
      )
    }

    return this.props.children
  }
}
```

#### 8.2 添加加载状态
在数据加载时显示加载指示器。

#### 8.3 添加离线检测
检测网络状态，提示用户。

---

### 阶段 9: 测试 (2小时)

#### 9.1 功能测试清单
- [ ] 用户注册/登录
- [ ] 创建新对话
- [ ] 加载对话列表
- [ ] 创建/编辑/删除笔记
- [ ] 发送消息
- [ ] 流式响应
- [ ] 上传文件
- [ ] 删除文件
- [ ] 删除对话
- [ ] 数据隔离（多用户测试）

#### 9.2 多用户测试
1. 创建两个测试账号
2. 分别创建对话和笔记
3. 验证数据隔离（用户A看不到用户B的数据）

#### 9.3 性能测试
- 大量笔记加载性能
- 防抖保存是否生效
- 流式消息更新性能

---

### 阶段 10: 部署准备 (1小时)

#### 10.1 环境变量配置
**开发环境** (`frontend/.env`):
```env
VITE_GEMINI_API_KEY=dev_key
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=dev_anon_key
```

**生产环境** (部署平台环境变量):
- 设置相同的环境变量
- 确保 Supabase URL 和 Key 正确

#### 10.2 构建优化
- 检查 `vite.config.ts` 构建配置
- 确保生产构建正常

#### 10.3 部署检查清单
- [ ] 环境变量已配置
- [ ] Supabase RLS 策略已启用
- [ ] 数据库连接正常
- [ ] 前端构建成功
- [ ] API 端点可访问

---

## 📝 代码修改清单

### 需要修改的文件

1. **新增文件**:
   - `frontend/src/lib/supabaseClient.ts` - Supabase 客户端
   - `frontend/src/services/conversationService.ts` - 对话服务
   - `frontend/src/services/noteService.ts` - 笔记服务
   - `frontend/src/services/messageService.ts` - 消息服务
   - `frontend/src/services/fileDbService.ts` - 文件服务
   - `frontend/src/utils/migrateLocalStorage.ts` - 迁移工具
   - `frontend/src/components/ErrorBoundary.tsx` - 错误边界

2. **需要修改的文件**:
   - `frontend/src/components/AuthPage.tsx` - 认证逻辑
   - `frontend/src/App.tsx` - 主应用逻辑（大量修改）
   - `frontend/package.json` - 添加依赖

3. **需要删除的代码**:
   - 所有 `localStorage.getItem/setItem/removeItem` 调用（除了布局偏好）
   - Mock 用户数据逻辑

---

## ⚠️ 注意事项

### 1. 数据迁移
- 首次迁移时，保留 localStorage 作为备份
- 迁移完成后，可以提示用户清理 localStorage
- 考虑提供"导出数据"功能

### 2. 性能优化
- 笔记保存使用防抖（500ms）
- 大量数据使用分页加载
- 考虑使用 React Query 缓存数据

### 3. 安全性
- 确保 RLS 策略正确配置
- 不在客户端暴露敏感信息
- 使用环境变量管理密钥

### 4. 错误处理
- 网络错误重试机制
- 友好的错误提示
- 离线状态处理

### 5. 向后兼容
- 支持从 localStorage 迁移
- 平滑过渡，不丢失数据

---

## 📊 时间估算

| 阶段 | 任务 | 预计时间 |
|------|------|----------|
| 1 | 环境准备 | 30分钟 |
| 2 | 数据库初始化 | 20分钟 |
| 3 | 用户认证迁移 | 1小时 |
| 4 | 对话管理迁移 | 2小时 |
| 5 | App.tsx 集成 | 3小时 |
| 6 | 数据迁移工具 | 1小时 |
| 7 | UI 状态管理 | 30分钟 |
| 8 | 错误处理优化 | 1小时 |
| 9 | 测试 | 2小时 |
| 10 | 部署准备 | 1小时 |
| **总计** | | **约 12 小时** |

---

## 🚀 开始实施

**建议顺序**:
1. ✅ 先完成阶段 1-2（环境准备和数据库初始化）
2. ✅ 然后阶段 3（用户认证）- 这是基础
3. ✅ 接着阶段 4-5（数据迁移和集成）
4. ✅ 最后阶段 6-10（优化和测试）

**下一步**: 确认计划后，我们可以开始实施。建议从阶段 1 开始。

